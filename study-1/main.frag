// ========================================
// パーティクル用フラグメントシェーダー
// 各パーティクル（ポイント）のピクセルの色を決定
// ========================================

// 浮動小数点の精度を中精度に設定
// mediump: 中精度（モバイルでもパフォーマンスと品質のバランスが良い）
precision mediump float;

// Varying変数: 頂点シェーダーから受け取る値
// 頂点シェーダーで設定された色がここに渡される
varying vec4 vColor;

void main() {
  // ========================================
  // パーティクルの色をそのまま出力
  // ========================================
  // 頂点シェーダーから渡された色をそのままピクセルの色として設定
  // gl_FragColor: 最終的なピクセルの色（RGBA: 赤, 緑, 青, 透明度）
  gl_FragColor = vColor;
}
